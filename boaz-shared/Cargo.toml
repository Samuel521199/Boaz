[package]
name = "boaz-shared"
version = "0.1.0"
edition = "2021"
authors = ["Samuel"]
description = "Boaz Daemon 与 UI 通信的 IPC 数据结构"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
